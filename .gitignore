# Git root is web/ — monorepo layout: app/ (SvelteKit), docs/, deploy/

# Cursor IDE — local only; do not commit (team members maintain their own copies)
.cursor/

deploy/compose/.env

app/node_modules/
app/.svelte-kit/
app/build/
app/.output/
app/.vercel/
app/.env
app/.env.*
!app/.env.example
!app/.env.test

.DS_Store
Thumbs.db
